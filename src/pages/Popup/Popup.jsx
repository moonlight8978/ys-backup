import React from 'react';
import Button from '../../components/Button/Button';
import './Popup.css';

const Popup = () => {
  return (
   <div>
     <Button onClick={() => {}}>Upload progress</Button>
     <Button onClick={() => {}}>Download progress</Button>
   </div>
  );
};

export default Popup;
